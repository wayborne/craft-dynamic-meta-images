<!doctype html>
<html>
    <head>
        <meta charset="utf-8">
        {# Tailwind #}
        <script src="https://cdn.tailwindcss.com"></script>
        {# Google fonts usage#}
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Poetsen+One&display=swap" rel="stylesheet">
        <style>
        .poetsen {
            font-family: "Poetsen One", sans-serif;
            font-weight: 400;
            font-style: normal;
        }
        </style>
        {# Title tag will be used to name the file #}
        <title>{{ entry.title }}</title>
    </head>
    <body class="min-w-screen min-h-screen bg-blue-400 p-12 bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500">
        <div class="absolute bottom-12 left-12 text-white w-[1/2] z-10">
        <h1 class="font-black lowercase text-[110px] leading-none poetsen">{{ entry.title }}</h1>
        <p class="text-[32px] mt-4 leading-none">{{ entry.postDate|date('d M Y') }}</p>
        </div>
    </body>
</html>
