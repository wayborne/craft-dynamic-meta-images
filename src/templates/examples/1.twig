<html>
<head>
    <meta charset="utf-8">
    {# Tailwind CSS #}
    <script src="https://cdn.tailwindcss.com"></script>
    {# Google fonts #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
    <style>
    .font-roboto{
        font-family: "Roboto", sans-serif;
    }
    </style>
    {# Title tag will be used to name the file #}
    <title>{{ entry.title }}</title>
</head>
<body class="min-w-screen min-h-screen p-12 text-black font-roboto">
    <div class="absolute bottom-12 left-12 w-[1/2] z-10">
        <p class="text-lg mb-4 font-bold uppercase">Company Logo</p>
        <h1 class="font-black lowercase text-[110px] leading-none  max-w-[60%]">{{ entry.title }}</h1>
        <p class="text-[32px] mt-8 leading-none">{{ entry.postDate|date('d M Y') }}</p>
    </div>
</body>
</html>
